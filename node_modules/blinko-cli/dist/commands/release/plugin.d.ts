import { Command } from '@oclif/core';
export default class ReleasePlugin extends Command {
    static description: string;
    static flags: {
        dir: import("@oclif/core/interfaces").OptionFlag<string, import("@oclif/core/interfaces").CustomOptions>;
    };
    static examples: string[];
    displayError(message: string): void;
    checkRequiredFiles(baseDir: string): void;
    openBrowser(url: string): Promise<void>;
    checkAndInstallGitHubCLI(): Promise<void>;
    checkReleaseExists(version: string): Promise<boolean>;
    submitPluginIssue(pluginUrl: string): Promise<void>;
    run(): Promise<void>;
}
