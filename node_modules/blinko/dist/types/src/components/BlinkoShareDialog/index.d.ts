interface ShareDialogProps {
    defaultSettings: ShareSettings;
    shareUrl?: string;
}
export interface ShareSettings {
    expiryDate?: Date;
    password?: string;
    shareUrl?: string;
    isShare?: boolean;
}
export declare const BlinkoShareDialog: (({ defaultSettings }: ShareDialogProps) => import("react").JSX.Element) & {
    displayName: string;
};
export {};
