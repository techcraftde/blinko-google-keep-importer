import React from 'react';
interface NoteHistoryModalProps {
    noteId: number;
}
declare const NoteHistoryModal: (({ noteId }: NoteHistoryModalProps) => React.JSX.Element) & {
    displayName: string;
};
export default NoteHistoryModal;
