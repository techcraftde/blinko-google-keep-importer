import { BlinkoStore } from '@/store/blinkoStore';
import { Note } from '@/server/types';
import { AvatarAccount } from './commentButton';
interface CardHeaderProps {
    blinkoItem: Note;
    blinko: BlinkoStore;
    isShareMode: boolean;
    isExpanded?: boolean;
    account?: AvatarAccount;
}
export declare const CardHeader: ({ blinkoItem, blinko, isShareMode, isExpanded, account }: CardHeaderProps) => import("react").JSX.Element;
export {};
