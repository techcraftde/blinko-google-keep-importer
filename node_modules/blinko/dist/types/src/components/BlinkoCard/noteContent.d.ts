import { Note } from '@/server/types';
import { BlinkoStore } from '@/store/blinkoStore';
interface NoteContentProps {
    blinkoItem: Note;
    blinko: BlinkoStore;
    isExpanded?: boolean;
    isShareMode?: boolean;
}
export declare const NoteContent: (({ blinkoItem, blinko, isExpanded, isShareMode }: NoteContentProps) => import("react").JSX.Element) & {
    displayName: string;
};
export {};
