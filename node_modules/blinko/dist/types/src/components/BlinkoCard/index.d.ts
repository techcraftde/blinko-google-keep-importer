import { Note } from '@/server/types';
import { AvatarAccount } from "./commentButton";
export type BlinkoItem = Note & {
    isBlog?: boolean;
    blogCover?: string;
    title?: string;
    originURL?: string;
};
interface BlinkoCardProps {
    blinkoItem: BlinkoItem;
    className?: string;
    account?: AvatarAccount;
    isShareMode?: boolean;
    forceBlog?: boolean;
    defaultExpanded?: boolean;
    glassEffect?: boolean;
    withoutHoverAnimation?: boolean;
}
export declare const BlinkoCard: (({ blinkoItem, account, isShareMode, glassEffect, forceBlog, withoutHoverAnimation, className }: BlinkoCardProps) => import("react").JSX.Element) & {
    displayName: string;
};
export {};
