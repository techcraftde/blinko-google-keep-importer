type IProps = {
    mode: 'create' | 'edit';
    onSended?: () => void;
    onHeightChange?: (height: number) => void;
    height?: number;
    isInDialog?: boolean;
};
export declare const BlinkoEditor: (({ mode, onSended, onHeightChange, isInDialog }: IProps) => import("react").JSX.Element) & {
    displayName: string;
};
export {};
