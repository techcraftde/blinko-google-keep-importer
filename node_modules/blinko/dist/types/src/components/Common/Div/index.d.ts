import { ReactNode } from 'react';
interface ClickWrapperProps {
    children: ReactNode;
    onTap: () => void;
    className?: string;
}
export declare const Div: ({ children, onTap, className }: ClickWrapperProps) => import("react").JSX.Element;
export {};
