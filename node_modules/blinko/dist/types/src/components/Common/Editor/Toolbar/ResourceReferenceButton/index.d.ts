import { EditorStore } from '../../editorStore';
interface Props {
    store: EditorStore;
}
export declare const ResourceReferenceButton: (({ store }: Props) => import("react").JSX.Element) & {
    displayName: string;
};
export {};
