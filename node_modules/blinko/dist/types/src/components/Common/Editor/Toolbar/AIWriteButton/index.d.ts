import { EditorStore } from '../../editorStore';
interface Props {
    store: EditorStore;
    content: string;
}
export declare const AIWriteButton: (({ store, content }: Props) => import("react").JSX.Element) & {
    displayName: string;
};
export {};
