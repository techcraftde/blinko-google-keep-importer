import { EditorStore } from '../../editorStore';
interface Props {
    store: EditorStore;
    content: string;
}
export declare const HashtagButton: (({ store, content }: Props) => import("react").JSX.Element) & {
    displayName: string;
};
export {};
