import { EditorStore } from '../../editorStore';
interface Props {
    store: EditorStore;
    isSendLoading?: boolean;
}
export declare const SendButton: (({ store, isSendLoading }: Props) => import("react").JSX.Element) & {
    displayName: string;
};
export {};
