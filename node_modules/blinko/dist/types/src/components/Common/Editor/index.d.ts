import "vditor/dist/index.css";
import '@/styles/vditor.css';
import React, { ReactElement } from 'react';
import { OnSendContentType } from './type';
import { type Attachment } from '@/server/types';
type IProps = {
    mode: 'create' | 'edit' | 'comment';
    content: string;
    onChange?: (content: string) => void;
    onHeightChange?: () => void;
    onSend: (args: OnSendContentType) => Promise<any>;
    isSendLoading?: boolean;
    bottomSlot?: ReactElement<any, any>;
    originFiles?: Attachment[];
    originReference?: number[];
    hiddenToolbar?: boolean;
};
declare const Editor: (({ content, onChange, onSend, isSendLoading, originFiles, originReference, mode, onHeightChange, hiddenToolbar }: IProps) => React.JSX.Element) & {
    displayName: string;
};
export default Editor;
