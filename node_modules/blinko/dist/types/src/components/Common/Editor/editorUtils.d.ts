import { Attachment } from "@/server/types";
import { FileType } from "./type";
import Vditor from "vditor";
export type ViewMode = "wysiwyg" | "sv" | "ir";
export type ToolbarProps = {
    store: any;
    files: FileType[];
    openFileDialog: () => void;
    getInputProps: any;
    mode: 'create' | 'edit';
    isPc: boolean;
    canSend: boolean;
    isSendLoading?: boolean;
    onSend?: (args: any) => Promise<any>;
    onChange?: (content: string) => void;
};
export type UploadAction = {
    key: string;
    icon: React.ReactNode;
    title: string;
    onClick: () => void;
    showCondition?: boolean;
};
export declare const HandleFileType: (originFiles: Attachment[]) => FileType[];
export declare const getEditorElements: (mode: ViewMode, editor: Vditor) => HTMLPreElement | undefined;
export declare const FocusEditorFixMobile: () => void;
