import React from 'react';
import 'katex/dist/katex.min.css';
export declare const MarkdownRender: (({ content, onChange, isShareMode }: {
    content?: string;
    onChange?: (newContent: string) => void;
    isShareMode?: boolean;
}) => React.JSX.Element) & {
    displayName: string;
};
export declare const StreamingCodeBlock: (({ markdown }: {
    markdown: string;
}) => React.JSX.Element) & {
    displayName: string;
};
