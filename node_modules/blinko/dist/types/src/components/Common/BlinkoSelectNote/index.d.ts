interface Props {
    iconButton?: React.ReactNode;
    onSelect: (item: any) => void;
    blackList?: number[];
    tooltip?: string;
    autoClose?: boolean;
}
export declare const BlinkoSelectNote: (({ iconButton, onSelect, blackList, tooltip, autoClose }: Props) => import("react").JSX.Element) & {
    displayName: string;
};
export {};
