import React from 'react';
import { FileType } from '../Editor/type';
export declare const DeleteIcon: (({ className, file, files, size }: {
    className: string;
    file: FileType;
    files: FileType[];
    size?: number;
}) => React.JSX.Element) & {
    displayName: string;
};
export declare const InsertConextButton: (({ className, file, files, size }: {
    className: string;
    file: FileType;
    files: FileType[];
    size?: number;
}) => React.JSX.Element) & {
    displayName: string;
};
export declare const DownloadIcon: (({ className, file, size }: {
    className?: string;
    file: FileType;
    size?: number;
}) => React.JSX.Element) & {
    displayName: string;
};
