import React from 'react';
import { type Attachment } from '@/server/types';
import { FileType } from '../Editor/type';
import { EditorStore } from '../Editor/editorStore';
type IProps = {
    files: FileType[];
    preview?: boolean;
    columns?: number;
    onReorder?: (newFiles: FileType[]) => void;
};
declare const AttachmentsRender: ((props: IProps) => React.JSX.Element) & {
    displayName: string;
};
declare const FilesAttachmentRender: (({ files, preview, columns, onReorder }: {
    files: Attachment[];
    preview?: boolean;
    columns?: number;
    onReorder?: (newFiles: Attachment[]) => void;
}) => React.JSX.Element) & {
    displayName: string;
};
declare const ReferenceRender: (({ store }: {
    store: EditorStore;
}) => React.JSX.Element) & {
    displayName: string;
};
export { AttachmentsRender, FilesAttachmentRender, ReferenceRender };
