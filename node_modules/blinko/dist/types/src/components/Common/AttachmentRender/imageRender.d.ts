import React from 'react';
import { FileType } from '../Editor/type';
type IProps = {
    files: FileType[];
    preview?: boolean;
    columns?: number;
    onReorder?: (newFiles: FileType[]) => void;
};
export declare const ImageThumbnailRender: ({ src, className }: {
    src: string;
    className?: string;
}) => React.JSX.Element;
declare const ImageRender: ((props: IProps) => React.JSX.Element) & {
    displayName: string;
};
export { ImageRender };
