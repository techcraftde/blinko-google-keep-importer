import React from 'react';
import { FileType } from '../Editor/type';
type DraggableFileGridProps = {
    files: FileType[];
    preview?: boolean;
    columns?: number;
    onReorder?: (newFiles: FileType[]) => void;
    type: 'image' | 'other';
    className?: string;
    renderItem?: (file: FileType) => React.ReactNode;
};
export declare const DraggableFileGrid: ({ files, preview, onReorder, type, className, renderItem }: DraggableFileGridProps) => React.JSX.Element;
export {};
