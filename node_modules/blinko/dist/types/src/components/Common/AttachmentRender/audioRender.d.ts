import React from 'react';
import { FileType } from '../Editor/type';
interface Props {
    files: FileType[];
    preview?: boolean;
}
export declare const AudioRender: (({ files, preview }: Props) => React.JSX.Element) & {
    displayName: string;
};
export {};
