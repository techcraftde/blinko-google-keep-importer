import { Store } from "./standard/base";
import { useRouter } from "next/router";
import { BlinkoStore } from "./blinkoStore";
import { ResourceType } from "@/server/types";
export declare class ResourceStore implements Store {
    sid: string;
    currentFolder: string | null;
    selectedItems: Set<number>;
    contextMenuResource: ResourceType | null;
    refreshTicker: number;
    clipboard: {
        type: 'cut' | 'copy';
        items: ResourceType[];
    } | null;
    constructor();
    get blinko(): BlinkoStore;
    setCurrentFolder: (folder: string | null) => void;
    selectAllFiles: (resources: ResourceType[]) => void;
    toggleSelect: (id: number) => void;
    clearSelection: () => void;
    loadResources: (folder?: string) => void;
    loadNextPage: () => void;
    handleDragEnd: (result: any) => Promise<void>;
    navigateToFolder: (folderName: string, router: ReturnType<typeof useRouter>) => Promise<void>;
    navigateBack: (router: ReturnType<typeof useRouter>) => Promise<void>;
    setContextMenuResource: (resource: ResourceType | null) => void;
    setCutItems: (items: ResourceType[]) => void;
    clearClipboard: () => void;
    use(router: any): void;
    handleNewFolder: () => void;
    moveToParentFolder: (items: ResourceType[]) => Promise<void>;
}
