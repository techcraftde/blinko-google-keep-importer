import { User } from 'next-auth';
import { Store } from './standard/base';
import { PromiseState } from './standard/PromiseState';
import { BlinkoStore } from './blinkoStore';
export declare class UserStore implements User, Store {
    sid: string;
    constructor();
    id: string;
    name?: string;
    nickname?: string;
    image?: string;
    role: string;
    theme: any;
    isSetup: boolean;
    languageInitialized: boolean;
    themeInitialized: boolean;
    isHubInitialized: boolean;
    isUseAIInitialized: boolean;
    wait(): Promise<UserStore>;
    get isSuperAdmin(): boolean;
    static wait(): Promise<UserStore>;
    get isLogin(): boolean;
    get blinko(): BlinkoStore;
    userInfo: PromiseState<(id: number) => Promise<{
        id: number;
        name: string;
        role: string;
        image: string | null;
        loginType: string;
        nickName: string;
        token: string;
        isLinked: boolean;
    }>, Promise<{
        id: number;
        name: string;
        role: string;
        image: string | null;
        loginType: string;
        nickName: string;
        token: string;
        isLinked: boolean;
    }>>;
    canRegister: PromiseState<() => Promise<boolean>, Promise<boolean>>;
    setData(args: Partial<UserStore>): void;
    ready(args: Partial<UserStore>): void;
    clear(): void;
    updatePWAColor(theme: string): void;
    initializeSettings(setTheme: (theme: string) => void, i18n: any): Promise<void>;
    use(): void;
}
