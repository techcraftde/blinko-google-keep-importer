import { Store } from './standard/base';
import { FileType } from '@/components/Common/Editor/type';
export interface AudioMetadata {
    coverUrl?: string;
    trackName?: string;
    albumName?: string;
    artists?: string[];
    previewUrl?: string;
}
export interface PlaylistTrack {
    file: FileType;
    metadata?: AudioMetadata;
    addedAt: number;
}
export declare class MusicManagerStore implements Store {
    sid: string;
    private static instance;
    private static audioInstance;
    constructor();
    private _playlist;
    private _currentTrackName;
    private _playMode;
    get playlist(): PlaylistTrack[];
    isPlaying: boolean;
    currentTime: number;
    duration: number;
    audioElement: HTMLAudioElement | null;
    showMiniPlayer: boolean;
    get playMode(): number;
    set playMode(value: number);
    get currentTrack(): PlaylistTrack | undefined;
    get currentIndex(): number;
    initAudio(audio: HTMLAudioElement): void;
    private removeAudioEvents;
    private handleTimeUpdate;
    private handleEnded;
    private handleLoadedMetadata;
    private handleError;
    private bindAudioEvents;
    addToPlaylist(file: FileType, metadata?: AudioMetadata, playNow?: boolean): void;
    setPlaylist(files: FileType[], metadata?: Record<string, AudioMetadata>): void;
    private mergePlaylist;
    removeFromPlaylist(trackName: string): void;
    playTrackByName(trackName: string): Promise<void>;
    togglePlay(): Promise<void>;
    playNext(): void;
    playPrevious(): void;
    seek(time: number): void;
    togglePlayMode(): void;
    closeMiniPlayer(): void;
    clearPlaylist(): void;
}
