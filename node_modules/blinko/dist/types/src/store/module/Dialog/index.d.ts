import React from "react";
import { ModalSlots, SlotsToClasses } from "@heroui/react";
import { Store } from "@/store/standard/base";
export declare class DialogStore implements Store {
    sid: string;
    provider: () => React.JSX.Element;
    isOpen: boolean;
    preventClose: boolean;
    placement: "center" | "auto" | "top" | "bottom" | "top-center" | "bottom-center";
    title: string;
    noPadding: boolean;
    size: "sm" | "md" | "lg" | "xl" | "2xl" | "full" | "xs" | "3xl" | "4xl" | "5xl" | 'full';
    onlyContent: boolean;
    showOnlyContentCloseButton: boolean;
    className: string;
    transparent: boolean;
    classNames?: SlotsToClasses<ModalSlots>;
    theme: string;
    content: React.ReactNode | ((props: any) => React.ReactNode);
    isDismissable: boolean;
    constructor(args?: Partial<DialogStore>);
    setData(v: Partial<DialogStore>): void;
    close(): void;
    static show(v: Partial<DialogStore>): void;
    static close(): void;
}
