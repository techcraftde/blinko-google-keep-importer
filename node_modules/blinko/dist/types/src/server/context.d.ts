import type * as trpcNext from '@trpc/server/adapters/next';
import Bowser from 'bowser';
export type User = {
    name: string;
    sub: string;
    role: string;
    id: string;
    exp: number;
    iat: number;
    ip?: string;
    userAgent?: Bowser.Parser.ParsedResult;
};
export declare function createContext(opts: trpcNext.CreateNextContextOptions): Promise<User | {
    id: string;
    ip: any;
    userAgent: Bowser.Parser.ParsedResult | null;
    name: string;
    sub: string;
    role: string;
    exp: number;
    iat: number;
}>;
export type Context = Awaited<ReturnType<typeof createContext>>;
