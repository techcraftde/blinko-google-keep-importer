import { Plugin } from 'vite';
export interface BlinkoPluginOptions {
    /**
     * Custom entry point for the plugin
     * @default 'src/index.tsx'
     */
    entry?: string;
    /**
     * Output directory for production build
     * @default 'release'
     */
    prodOutDir?: string;
    /**
     * Output directory for development build
     * @default 'dist'
     */
    devOutDir?: string;
    /**
     * Required fields for plugin.json validation
     * @default ['name', 'author', 'url', 'version', 'minAppVersion', 'displayName', 'description']
     */
    requiredFields?: string[];
    /**
     * Path to the plugin.json file
     * @default process.cwd() + '/plugin.json'
     */
    pluginJsonPath?: string;
    /**
     * External dependencies
     * @default ['blinko']
     */
    externals?: string[];
    /**
     * Whether to use random filename for development builds
     * @default true
     */
    useRandomFilename?: boolean;
}
/**
 * Vite plugin for Blinko application development
 * @param options - Plugin options
 * @returns Vite plugin
 */
export default function blinkoPlugin(options?: BlinkoPluginOptions): Plugin[];
